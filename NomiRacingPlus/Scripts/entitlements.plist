<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Hardened Runtime - required for notarization -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Allow unsigned executable memory (needed by some game engines) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Allow loading of plugins -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Network access for multiplayer/online features -->
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>

    <!-- Audio input (for voice chat if needed) -->
    <key>com.apple.security.device.audio-input</key>
    <true/>

    <!-- USB device access (for game controllers) -->
    <key>com.apple.security.device.usb</key>
    <true/>

    <!-- Game controller access -->
    <key>com.apple.security.device.gamecontroller</key>
    <true/>
</dict>
</plist>
